What is GIT?
  is a version conrol system which is open source
  basically version control is a practice of tracking & managing changes made to files.
  open-source

  main feature of git is 
    distributed - which means each developer has its own copy.



package.json
    contains all data in key value pair.
    we can say that package.json is the heart of our application. 
    exist at root level of our application.
    contains entry point to our application.
    also we can have scripts & dependencies related to app inside this file.
  

.gitignore
    it is a normal text file which tells git which files to ignore when commiting a project on github.
    this is also placed at root directory of app.
